name: neurofuzzy
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy>=1.24
  - scipy>=1.10
  - pandas>=2.0
  - matplotlib>=3.7
  - jupyter
  - pip
  - pip:
      # Install the package itself (editable) from the repository root.
      - -e .
