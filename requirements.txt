# Core runtime dependencies for the neurofuzzy package and benchmark harness.
# For an exact, reproducible pin set see requirements-lock.txt.
numpy>=1.24
scipy>=1.10
pandas>=2.0
# Optional (figures): install with `pip install -e ".[viz]"`
# matplotlib>=3.7
